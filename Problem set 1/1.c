#include <stdio.h>
int main(){
    int a, b,c, result;
    printf("Enter 3 digit : ");
    scanf("%d%d%d", &a, &b, &c);
    result = a * b * c ;
    printf("result %d",result);
    return 0;
}
